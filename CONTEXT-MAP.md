# Context map

This repository has two engineering contexts:

| Context | Scope | Documentation |
| --- | --- | --- |
| Rust/Dioxus application | The fullstack blog/CMS, server functions, database, SSR, and admin pages | [`src/CONTEXT.md`](src/CONTEXT.md) |
| JavaScript workspace libraries | IIFE browser bundles under `libs/` and their generated `public/` assets | [`libs/CONTEXT.md`](libs/CONTEXT.md) |

Read only the context relevant to the current change. Context files and scoped ADRs are added when their vocabulary or decisions are first resolved.

System-wide architectural decisions live in [`docs/adr/`](docs/adr/).
